"""
Command-line interface package.

This package contains the CLI entry point and related utilities for
executing the RAG ingestion pipeline.

Planned commands:

- ingest
- validate
- version

The CLI should remain lightweight and delegate all business logic to the
application layer through the composition root.
"""

__all__: list[str] = []